model:
  name: "mock-vlm"
  path: null
  device: "auto"

image:
  # VLM preprocessing for RGB frames extracted from a RealSense snapshot.
  max_size: 1024
  quality: 85

paths:
  snapshot_replay: "data/realsense_snapshots"
  vlm_cache: "data/processed/auto"
  results: "outputs/results"
  logs: "outputs/logs"
