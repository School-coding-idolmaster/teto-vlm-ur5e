# Calibration Schema Boundary

This directory is reserved for future schema notes for camera-to-base TF,
RealSense D455 extrinsics, timestamped calibration snapshots, and projection
metadata.

H8 does not add runtime dataclasses, validators, imports, or call sites here.
Existing calibration-like contracts remain in their current modules until a
later audited migration.
