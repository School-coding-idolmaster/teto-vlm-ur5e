# Replay Schema Boundary

This directory is reserved for future schema notes for replay manifests,
snapshot replay bundles, semantic replay records, and evidence indexes.

H8 does not add runtime dataclasses, validators, imports, or call sites here.
Existing replay and evidence code remains in its current modules until a later
audited migration.
